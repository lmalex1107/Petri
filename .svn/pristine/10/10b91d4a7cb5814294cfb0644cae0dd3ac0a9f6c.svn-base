package control;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.util.ArrayList;

import model.Arc;
import model.Knot;
import view.GUI;
import view.PetriGraph;

public class Control implements ActionListener{
	ArrayList<Knot> knotlist = null;
	ArrayList<Arc> arclist = null;
	GUI gui = new GUI(this, knotlist, arclist);
	PetriGraph petrigraph;
	
	
	
	@Override
	public void actionPerformed(ActionEvent ae) {
		if (ae.getSource() == gui.oeffnen) {
			gui.open();
			this.petrigraph = gui.getPetriGraph();
			gui.centerpanel.graphpanel.petripanel.showGraph(petrigraph);
		}
		
		if (ae.getSource() == gui.neu_laden) {
			
		}
		
		if (ae.getSource() == gui.analyse) {
			
		}
		
		if (ae.getSource() == gui.beenden) {
			gui.exit();
		}
		
	}
	
	public static void main (String args[]) {
		
		javax.swing.SwingUtilities.invokeLater(new Runnable() {
			public void run() {
				Control control = new Control();
			}
		});
		
	}
	
	
}
