package view;

import java.awt.BorderLayout;
import java.util.ArrayList;

import javax.swing.JPanel;

import model.Arc;
import model.Knot;

public class CenterPanel extends JPanel {
	public CenterPanel (ArrayList<Knot> knotlist, ArrayList<Arc> arclist) {
		this.setLayout(new BorderLayout());
		SymbolPanel symbolpanel = new SymbolPanel();
		GraphPanel graphpanel = new GraphPanel(knotlist, arclist);
		this.add(symbolpanel, BorderLayout.NORTH);
		this.add(graphpanel, BorderLayout.SOUTH);
	}
}
